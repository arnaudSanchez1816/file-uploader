<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>File Uploader</title>
    </head>
    <body>
        <h1>Hello world !</h1>
        <p>Home</p>
        <% if (locals.user) { %>
            <p>Welcome <%= locals.user.email %>.</p>
            <a href="/signout">Sign out</a>
        <% } else { %>
            <a href="/signin">Sign in</a>
            <a href="/signup">Sign up</a>
        <% } %>
    </body>
</html>
